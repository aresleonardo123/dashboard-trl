name: 🚀 Deploy Docker App to Azure Web App

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Clonar repositorio
        uses: actions/checkout@v3

      - name: 📦 Configurar Docker build
        run: |
          docker build -t myapp:latest .

      - name: 🚀 Deploy a Azure Web App con perfil de publicación
        uses: azure/webapps-deploy@v2
        with:
          app-name: ares
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_54D069DACC084CB4B82067EF66D66C2A }}
          images: myapp:latest
