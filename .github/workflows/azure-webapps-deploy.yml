name: 🚀 Deploy con Publish Profile

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Clonar repositorio
        uses: actions/checkout@v3

      - name: 🚀 Desplegar Dockerfile a Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: ares  # nombre exacto de tu Web App
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_54D069DACC084CB4B82067EF66D66C2A}}  # copia todo el nombre del secreto tal como aparece
          images: DOCKER|
